# Cloudflare Browser scripts

- `cdp-client.js` — WebSocket CDP helper
- `screenshot.js` — navigate + capture PNG
- `video.js` — multi-URL capture helper

Set `CDP_SECRET` and worker `cdpUrl`. See parent `SKILL.md`.

Full script sources live in the skill package shipped with Open-Connect releases; regenerate from Cloudflare Browser Rendering docs if needed.
